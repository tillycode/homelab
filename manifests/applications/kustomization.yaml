apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# TODO: refactor to public/private ingress/DNS/cert-manager
resources:
  - private-ingress.yaml
  - private-dns.yaml
  - public-ingress.yaml
  - public-dns.yaml
  - cert-manager.yaml
